export interface ProductCategoryDto {
    id: string;
    imageUrl: string;
    name: string;
    productGroupId: string;
}

export interface ProductCategoryTableDto {
    id: string;
    imageUrl: string;
    name: string;
    productGroup: string;
}